# use with sharing on the apex side

# use
- <sObject>.sObjectType.getDescription()
isCreateable()
isAccessible()
isUpdateable()
isDeletable()
Ex:
if(Account.sObjectType.getDescribe().isAccessible())
 return true;
